- Add control block logic for both the authenticator, key hash,
  encryptor, and compressor choices.

- srlog2:

  - Add option to set path to host key list

  - Add backoff on INI timing

- srlog2d:

  - Sanity check the sender and service names?

  - Add more counters for error packets

- srlog2-logger:

  - Add an option for formatting the directory name(s)?

  - Add options for size/count-based rotation (ala multilog)

- New program: srlog2-logsplit

  - Similar to -logger, but pipes logs into one process per incoming
    stream

- Merge common parts of hash tables in services.c and srlog2-logger.c
